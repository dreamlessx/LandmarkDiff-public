coverage:
  status:
    project:
      default:
        target: 73%
        threshold: 2%
    patch:
      default:
        target: 80%

ignore:
  - "scripts/**"
  - "examples/**"
  - "docs/**"
  - "tests/**"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
